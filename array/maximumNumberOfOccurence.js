//time complexity : O(n)

arr = [1,2,3,3,3];
N = arr.length;

console.log(majorityElement(arr, N));

function majorityElement(a, size)
{
    let occurence = N / 2
    let temp = []
    for(var i = 0; i < size; i++){
      if(temp.has(arr[i])){
        
      }
    }

    
}